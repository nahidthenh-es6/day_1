// Arrow Function

const sum = (a, b) => {
    const x = a + b;
    const y = a - b;
    return y, x;
};

// console.log(sum(112, 10)); 

// Truthy / falsy value ----------------------
let a = 0;

if (a) {
    // console.log('Truthy Value');
} else {
    // console.log('false Value');
}

// Ternary Operator -------------------------------
